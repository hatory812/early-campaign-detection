# 研究概要
## 背景

ソーシャルメディアにおいて、人為的な介入により特定のトピックを意図的にトレンド化させる組織的キャンペーンが問題視されている。これに対し、ユーザー間のリプライやリツイート等のエンゲージメントをカスケードグラフとして表現し、その構造的特徴から自然発生したトレンドと組織的キャンペーンを識別する手法が提案されている [1]。しかし、既存研究では、高精度な識別のためにカスケードグラフをどの程度の期間観測すべきかという点については十分に議論されてこなかった。早期検知の観点からは、必要な観測期間を明らかにすることは重要である。そこで本稿では、カスケードグラフの観測期間を変化させた際の組織的キャンペーンの検出精度を実験的に調査する。これにより、組織的キャンペーン検出に必要な最小限の観測期間について検討する。

## 実験方法

組織的キャンペーン検出手法として、既存フレームワークである DECODE (DEnsity-aware walks for COordinated campaign DEtection) [1] を用いる。DECODE は、加重ランダムウォークによるノード埋め込みとグラフニューラルネットワーク（GNN）を組み合わせることで、対象のカスケードグラフが「組織的キャンペーン」か「自然発生トレンド」かを識別する。本実験では、GNN として GCN (Graph Convolutional Network) を採用し、重み付け特徴量にはノード次数を用いた。

評価用データセットには、2023 年トルコ選挙におけるカスケードデータ [2] を使用した。これには ~ 件のキャンペーングラフと ~ 件の非キャンペーングラフが含まれる。グラフのノードはユーザー、リンクはリプライやリツイート等のエンゲージメントを表し、リンクにはタイムスタンプが付与されている。

`preprint_ecml_pkdd_2025_ads_1300.pdf`の2値分類を用いて別の実験をする。その内容は次のとおりである。
- グラフをある時点までのスナップショットのように縮小化した時、学習の精度にはどのような影響があるかを確かめたい。
- 入力するグラフ（/hss01/A.hattori/all_graphs）のエッジのタイムスタンプを用いる。
- 時間幅を表す変数をt_wとする。この変数には0, 1, 2, 3, … , 60の1刻みの数値が順に代入される。t_wが60になるまで以下を繰り返し。
    - 入力するグラフのデータセットをGとし、その中に含まれるグラフをG_iとする。つまり、G={G_i | i = 1,2,...}。すべてのG_iについて以下を繰り返し。
        - あるグラフG_iにおいて、最小のタイムスタンプをt_0とする。
        - あるグラフG_iを縮小化する時、タイムスタンプがt_0+t_w以内のエッジを抽出する。この抽出したエッジ集合をE_ieとする。
        - 抽出したエッジ集合E_eの両端に接続しているノードを抽出する。この抽出したノード集合をV_ieとする。
        - このE_ieとV_ieを要素としたグラフをG_i’とする。
        - G_i’に関して、加重ランダムウォークによるノード埋め込みを再計算する。
    - すべてのG_iに関するG_i’の集合をG’とする。つまり、G’={G_i’ | i = 1,2,...}。このG’を新たなグラフのデータセットとしてモデルに入力し、訓練する。
    - 訓練したモデルでAccuracy, Precision, Recall, F1-scoreを算出する。
つまり、学習モデルに渡す入力データが、情報の拡散開始から１分以内、２分以内、３分以内、、、の情報のみに縮小化した時、どのような精度になるのかを確かめる。

## 参考文献

[1] Gopalakrishnan, Atul Anand et al. "Density-aware Walks for Coordinated Campaign Detection." Proc. ECML/PKDD (2025).
[2] GOPALAKRISHNAN, Atul Anand, et al. Large Engagement Networks for Classifying Coordinated Campaigns and Organic Twitter Trends. In: Proceedings of the International AAAI Conference on Web and Social Media. 2025. p. 688-702
